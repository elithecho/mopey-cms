import { GlobalConfig } from 'payload/types';

const Home: GlobalConfig = {
  slug: 'home',
  label: 'Home page',
  fields: [
    {
      name: 'Text',
      type: 'text'
    },
  ],
}

export default Home
